# 全局 Claude Code 配置

## 查看上下文进度（内存占比）

- 对话底部状态栏实时显示当前 token 使用量
- 使用 `/status` 命令可查看当前会话详细信息
- 新会话开始时上下文占用极低（不到 1%）

## 工具使用规范

- 每完成一个功能设定或修改后，主动询问用户是否需要留存（如 git 提交、更新 MD 文档等），根据实际工具类型判断合适的留存方式

## 语言偏好

- 回答简洁，不啰嗦
- 涉及代码或专业术语时，需用大白话额外解释一句，方便零基础用户理解
